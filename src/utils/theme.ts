import { createTheme } from "@mui/material"

export const theme = createTheme({
  palette: {
    background: {
      default: "#FFFFFF",
      paper: "#585755",
    },
    primary: {
      main: "#131325",
      dark: "#212140",
    },
  },
})
